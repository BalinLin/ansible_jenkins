Vars:
  Docker:
    docker_home: "/docker"
    index_path: "/docker/index.html"
  Jenkins:
    docker_jenkins_image: "docker-jenkins"
    docker_jenkins_container: "docker-jenkins"
    docker_jenkins_home: "/docker/docker-jenkins"

    jenkins_server_url: "http://localhost"
    jenkins_port: 8080
    jenkins_url: "http://localhost:8080"

    jenkins_admin_user: "admin"
    jenkins_admin_pass: "admin"

    # apt_packages:
    #   - "python-pip"
    #   - "wget"
    #   - "unzip"

    # pip_packages:
    #   - "ansible-lint"
  Terraform:
    project_path: "/infra"
